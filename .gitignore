# scratch
temp/
*.tmp
*.log

# built output of this repository (documents only)
dist/
.cxbuild/

# the private machine layer: real boundary values live outside this repository.
# the convention is a sibling directory (<parent>\KalciriteThinking-local), which
# is already outside the tree; this entry is the safety net if one lands inside.
local/

# editors / OS
.vscode/
.idea/
Thumbs.db
desktop.ini
